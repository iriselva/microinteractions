import React from "react";
import Book from "../images/Book.svg";
import styled from "styled-components";

const Grid = styled.div`
  display: flex;
  fle

`;

export default function Order({ notebook }) {
  return (
    <div>
      <h4>Your order is....</h4>
    </div>
  );
}
